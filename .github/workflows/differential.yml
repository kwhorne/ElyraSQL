name: MySQL differential

# Runs an identical battery of edge-case queries against ElyraSQL and a real
# MySQL 8, and fails on any non-allowlisted divergence. This catches
# MySQL-semantics regressions before users hit them. Known intentional/tracked
# differences are allowlisted in the harness.
#
# Three things are compared. Rows was the original one; the other two were added
# after divergences got through it:
#   rows           values and NULLs, plus one engine accepting what the other rejects
#   column types   the declared type code -- a boolean in arithmetic came back as
#                  DOUBLE where MySQL sends BIGINT, and every value matched
#   affected rows  for the DML battery -- ON DUPLICATE KEY UPDATE reported 1 where
#                  MySQL reports 2, in five shapes, and nothing looked at the count

on:
  push:
    branches: [main]
  pull_request:
  schedule:
    - cron: "0 5 * * *" # nightly 05:00 UTC
  workflow_dispatch:

permissions:
  contents: read

jobs:
  differential:
    name: ElyraSQL vs MySQL 8
    runs-on: ubuntu-24.04

    services:
      mysql:
        image: mysql:8.4
        env:
          MYSQL_ROOT_PASSWORD: root
        ports:
          - 3390:3306
        options: >-
          --health-cmd="mysqladmin ping -h 127.0.0.1 -proot"
          --health-interval=5s --health-timeout=5s --health-retries=20

    steps:
      - uses: actions/checkout@v7

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2

      - name: Build ElyraSQL
        run: cargo build --release -p elyra-cli

      - name: Python driver
        run: pip install --quiet pymysql

      - name: Start ElyraSQL
        run: |
          ./target/release/elyrasql serve --data /tmp/diff.edb --listen 127.0.0.1:3307 &
          for i in $(seq 1 30); do
            python3 -c "import pymysql; pymysql.connect(host='127.0.0.1',port=3307,user='root')" 2>/dev/null && break
            sleep 1
          done

      - name: Differential
        run: |
          python3 tests/compat/differential/mysql_diff.py \
            --elyra-port 3307 --elyra-password '' \
            --ref-port 3390 --ref-user root --ref-password root | tee /tmp/diff.txt
          { echo '## MySQL differential'; echo '```'; cat /tmp/diff.txt; echo '```'; } >> "$GITHUB_STEP_SUMMARY"
